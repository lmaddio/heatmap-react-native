/**
 * Native platform map components using react-native-maps
 */
import MapView, { Circle, Polyline, Marker } from 'react-native-maps';

export { MapView, Circle, Polyline, Marker };
export default MapView;
